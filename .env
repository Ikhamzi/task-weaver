# Frontend has no build-time config — /api requests are proxied to the
# backend in dev (vite.config.ts) and served same-origin in production.
# All secrets (Gemini, Google OAuth, Postgres, Resend) live in server/.env.
